fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic>=2.9.2
pydantic-settings>=2.10.1
python-dotenv==1.0.1
# needed for FastAPI's request.form()/UploadFile (multipart parsing) — used
# by app/agent/agentsandbox_server.py's /upload endpoint.
python-multipart==0.0.12
