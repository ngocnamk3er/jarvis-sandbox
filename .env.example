APP_NAME=Jarvis Sandbox
APP_VERSION=0.2.0
API_PREFIX=/api/v1

# The one dir the command sees. In k8s an emptyDir (mounted by the
# SandboxTemplate); locally any writable dir.
WORKSPACE_DIR=/tmp/jarvis-sandbox/workspace
